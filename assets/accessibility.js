(function(){
  function currentFile(){var p=(location.pathname||'').split('/').filter(Boolean).pop()||'index.html';return p.toLowerCase();}
  function ensureHomeLink(){var links=document.querySelector('.moran-site-nav .moran-links');if(!links)return;var existing=links.querySelector('a[href="index.html"]');if(currentFile()==='index.html'){if(existing)existing.remove();return;}if(!existing){var a=document.createElement('a');a.href='index.html';a.textContent='ראשי';links.insertBefore(a,links.firstChild);}}
  function setupHealthForm(){
    var form=document.querySelector('form[name="health-declaration"]');if(!form)return;
    var steps=[].slice.call(form.querySelectorAll('.step')),bars=[].slice.call(document.querySelectorAll('#progress i')),current=steps.findIndex(function(s){return s.classList.contains('active')});if(current<0)current=0;var error=document.getElementById('error');
    function syncConditional(){form.querySelectorAll('.q').forEach(function(q){var yes=q.querySelector('input[type="radio"][value="כן"]'),details=q.querySelector('.details');if(!details)return;var active=!!(yes&&yes.checked);q.classList.toggle('yes',active);details.querySelectorAll('input,textarea,select').forEach(function(el){if(el.type!=='file')el.required=active;});});var file=form.querySelector('input[name="medical_document"]');if(file)file.required=false;}
    function show(n){current=n;steps.forEach(function(s,i){s.classList.toggle('active',i===current)});bars.forEach(function(b,i){b.classList.toggle('on',i<=current)});if(error)error.classList.remove('show');initSignature();window.scrollTo({top:100,behavior:'smooth'});}
    function validateCurrent(){syncConditional();var controls=[].slice.call(steps[current].querySelectorAll('input,textarea,select'));for(var i=0;i<controls.length;i++){var el=controls[i];if(el.type==='hidden'||el.type==='file')continue;var details=el.closest('.details');if(details&&getComputedStyle(details).display==='none')continue;if(!el.checkValidity()){if(error)error.classList.add('show');el.reportValidity();return false;}}if(error)error.classList.remove('show');return true;}
    form.querySelectorAll('input,textarea,select').forEach(function(el){if(el.type==='hidden'||el.type==='file'||el.type==='button'||el.type==='submit'||el.closest('.details'))return;el.required=true;});syncConditional();form.addEventListener('change',syncConditional);
    document.querySelectorAll('.next').forEach(function(btn){btn.onclick=function(){if(validateCurrent())show(Math.min(steps.length-1,current+1));};});document.querySelectorAll('.prev').forEach(function(btn){if(btn.id!=='clear')btn.onclick=function(){show(Math.max(0,current-1));};});
    var canvas=document.getElementById('sig'),ctx=null,draw=false,hasSig=false;
    function initSignature(){if(!canvas||!canvas.offsetParent)return;var r=canvas.getBoundingClientRect(),ratio=window.devicePixelRatio||1;canvas.width=Math.max(1,Math.round(r.width*ratio));canvas.height=Math.max(1,Math.round(r.height*ratio));ctx=canvas.getContext('2d');ctx.setTransform(ratio,0,0,ratio,0,0);ctx.lineWidth=2;ctx.lineCap='round';ctx.strokeStyle='#35322f';}
    function point(e){var r=canvas.getBoundingClientRect();return[e.clientX-r.left,e.clientY-r.top];}
    if(canvas){canvas.onpointerdown=function(e){initSignature();draw=true;hasSig=true;canvas.setPointerCapture(e.pointerId);ctx.beginPath();var p=point(e);ctx.moveTo(p[0],p[1]);var w=canvas.closest('.signature-wrap');if(w)w.classList.add('signed');e.preventDefault();};canvas.onpointermove=function(e){if(!draw)return;var p=point(e);ctx.lineTo(p[0],p[1]);ctx.stroke();e.preventDefault();};canvas.onpointerup=canvas.onpointercancel=function(){draw=false;};var clear=document.getElementById('clear');if(clear)clear.onclick=function(){initSignature();ctx.clearRect(0,0,canvas.width,canvas.height);hasSig=false;var w=canvas.closest('.signature-wrap');if(w)w.classList.remove('signed');};}
    function esc(v){return String(v||'').replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
    function buildCopy(){var rows=[];new FormData(form).forEach(function(v,k){if(k==='form-name'||k==='signature'||k==='medical_document'||v instanceof File)return;var el=form.elements[k],label='';if(el){var target=el.length?el[0]:el;var field=target.closest&&target.closest('.field,.q');var lab=field&&field.querySelector('label,strong');label=lab?lab.textContent.trim():k;}rows.push('<tr><th>'+esc(label||k)+'</th><td>'+esc(v)+'</td></tr>');});var sig=canvas&&hasSig?canvas.toDataURL('image/png'):'';return '<div dir="rtl" style="font-family:Arial,sans-serif;direction:rtl;color:#333;background:#fff;padding:28px;width:738px"><h1 style="text-align:center">הצהרת בריאות שנתית</h1><p>צהרון עיניים טובות</p><table style="width:100%;border-collapse:collapse;margin:24px 0">'+rows.join('')+'</table><h3>חתימת הורה / אפוטרופוס</h3>'+(sig?'<img style="max-width:320px;border:1px solid #ddd;padding:8px" src="'+sig+'">':'')+'<p>תאריך: '+esc(new Date().toLocaleString('he-IL'))+'</p></div>';}
    function loadPdfLibrary(){return new Promise(function(resolve,reject){if(window.html2pdf){resolve();return;}var script=document.createElement('script');script.src='https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js';script.onload=resolve;script.onerror=function(){reject(new Error('לא ניתן לטעון את רכיב יצירת ה-PDF'));};document.head.appendChild(script);});}
    async function createPdfBase64(html){await loadPdfLibrary();var box=document.createElement('div');box.setAttribute('dir','rtl');box.style.cssText='width:794px;background:#fff;padding:28px;color:#333;font-family:Arial,sans-serif;direction:rtl;position:absolute;top:0;left:0;z-index:-9999;opacity:0.01;pointer-events:none;';box.innerHTML=html;box.querySelectorAll('th,td').forEach(function(el){el.style.border='1px solid #ddd';el.style.padding='9px';el.style.textAlign='right';el.style.verticalAlign='top';});box.querySelectorAll('th').forEach(function(el){el.style.width='42%';el.style.background='#f7f2ef';});document.body.appendChild(box);try{await new Promise(function(r){requestAnimationFrame(function(){requestAnimationFrame(r);});});var imgs=[].slice.call(box.querySelectorAll('img'));await Promise.all(imgs.map(function(img){return img.complete?Promise.resolve():new Promise(function(r){img.onload=img.onerror=r;});}));var worker=window.html2pdf().set({margin:[8,8,8,8],image:{type:'jpeg',quality:0.98},html2canvas:{scale:2,useCORS:true,backgroundColor:'#ffffff',logging:false},jsPDF:{unit:'mm',format:'a4',orientation:'portrait'},pagebreak:{mode:['css','legacy']}}).from(box).toPdf();var pdf=await worker.get('pdf');var dataUri=pdf.output('datauristring');return dataUri.split(',')[1];}finally{box.remove();}}
    function downloadPdf(base64,childName){var bytes=atob(base64),arr=new Uint8Array(bytes.length);for(var i=0;i<bytes.length;i++)arr[i]=bytes.charCodeAt(i);var blob=new Blob([arr],{type:'application/pdf'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='הצהרת-בריאות-'+(childName||'ילד')+'.pdf';document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(url);},1000);}
    function fileToBase64(file){return new Promise(function(resolve,reject){if(!file){resolve('');return;}var reader=new FileReader();reader.onload=function(){resolve(String(reader.result||'').split(',')[1]||'');};reader.onerror=function(){reject(new Error('קריאת המסמך הרפואי נכשלה'));};reader.readAsDataURL(file);});}
    form.addEventListener('submit',async function(e){e.preventDefault();e.stopImmediatePropagation();if(!validateCurrent())return;if(canvas&&!hasSig){alert('יש לחתום בכתב יד לפני שליחת ההצהרה');return;}var sig=document.getElementById('signature');if(sig&&canvas)sig.value=canvas.toDataURL('image/png');var signedAt=document.getElementById('signedAt');if(signedAt)signedAt.value=new Date().toLocaleString('he-IL');var copyHtml=buildCopy(),submit=form.querySelector('button[type="submit"]');if(submit){submit.disabled=true;submit.textContent='מכין PDF ושולח...';}try{var parentEmail=(form.querySelector('[name="parent_email"]')||{}).value||'',childName=(form.querySelector('[name="child_name"]')||{}).value||'',medicalInput=form.querySelector('[name="medical_document"]'),medicalFile=medicalInput&&medicalInput.files&&medicalInput.files[0]?medicalInput.files[0]:null;var pdfBase64=await createPdfBase64(copyHtml);var medicalBase64=await fileToBase64(medicalFile);var mail=await fetch('/api/send-form-confirmation',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({parent_email:parentEmail,child_name:childName,pdf_base64:pdfBase64,medical_base64:medicalBase64,medical_filename:medicalFile?medicalFile.name:''})});var mailBody={};try{mailBody=await mail.json();}catch(_e){}if(!mail.ok)throw new Error(mailBody.error||'שליחת המייל נכשלה');form.innerHTML='<div style="text-align:center;padding:36px 14px"><div style="font-size:42px">✓</div><h2>ההצהרה נשלחה בהצלחה</h2><p>קובץ PDF נשלח למייל של הצהרון'+(mailBody.parentSent?' וגם להורה.':'.')+'</p><button type="button" id="download-copy" class="btn submit" style="margin-top:14px">הורדת עותק PDF</button></div>';var dl=document.getElementById('download-copy');if(dl)dl.onclick=function(){downloadPdf(pdfBase64,childName);};window.scrollTo({top:100,behavior:'smooth'});}catch(err){alert(err&&err.message?err.message:'אירעה שגיאה בשליחה. נסו שוב.');if(submit){submit.disabled=false;submit.textContent='חתימה ושליחת ההצהרה';}}},true);initSignature();
  }
  ensureHomeLink();setupHealthForm();var s=document.createElement('script');s.src='assets/accessibility-original.js';s.defer=true;document.head.appendChild(s);
})();